# Where the API server lives, with no trailing slash.
# Defaults to http://localhost:5001 when unset.
#
# This is read at BUILD time, not run time — set it in your host's environment
# variables and redeploy after changing it.
VITE_API_URL=http://localhost:5001
